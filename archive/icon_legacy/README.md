# Legacy Icon Workflow Archive

This directory preserves the previous repository icon workflow for reference.

## Archived Items

- `generate_app_icon.ps1`
- `icon_resize.py`
- optional legacy source samples moved from the old root-level icon workspace

## Why It Was Archived

The legacy flow no longer matches the current repository structure because:

- it centered on an older bitmap-generation path
- it did not export upload-ready icon artifacts into the repository artifact layout
- it did not reflect the current dedicated icon workflow and skill routing

## Current Standard

The active icon workflow is now defined by:

- `docs/GAME_GENERATION_STANDARD.md`
- `docs/ICON_WORKFLOW.md`
- `tools/assets/generate_app_icon.ps1`

Use the archived files only for historical reference or migration analysis.
