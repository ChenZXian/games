# Legacy Cursor Rules Archive

This directory preserves superseded `docs/cursor/` rules versions for reference.

Archived items here are historical snapshots.
They should not be treated as the active Cursor rules for the repository.

Active location:

- `docs/cursor/CURSOR_RULES_v1.5.md`

Archive policy:

- keep only the current active Cursor rules file under `docs/cursor/`
- move older versions here when they are replaced
- do not update archived copies unless they are being annotated for archival context
